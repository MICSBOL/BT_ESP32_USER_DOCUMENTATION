\chapter{Wiring Diagrams}

This chapter shows basic wiring examples for common projects using the ESP32 RC Controller firmware.

These diagrams are simplified so beginners can quickly understand how to connect the hardware.

\section{Basic Robot Wiring}

The most common project is a simple differential drive robot using two motors.

% Requires: \usetikzlibrary{positioning,arrows.meta,calc}
% (You already load positioning+arrows.meta in main.tex.)
% Narrower (less wide) + optionally taller layout.
% Uses a mostly-vertical flow: Phone -> ESP32 -> Driver, with motors branching right.
% Requires: \usetikzlibrary{positioning,arrows.meta,calc}

\begin{center}
    \begin{tikzpicture}[
        font=\small,
        >=Stealth,
        node distance=22mm and 34mm, % tighter spacing (vertical and horizontal)
        box/.style={
            draw,
            rounded corners=2mm,
            very thick,
            align=center,
            minimum width=36mm,  % reduced width
            minimum height=14mm, % slightly taller
            fill=gray!6
        },
        boxAccent/.style={
            box,
            fill=blue!6,
            draw=blue!60!black
        },
        boxHW/.style={
            box,
            fill=green!6,
            draw=green!50!black
        },
        arrow/.style={->, very thick, draw=black!70},
        label/.style={midway, fill=white, inner sep=1.5pt, text=black!80, font=\scriptsize}
        ]

        % Nodes (stacked vertically to reduce width)
        \node[boxAccent] (phone) {Android\\App};
        \node[boxAccent, below=of phone] (esp32) {ESP32\\Controller};
        \node[boxHW, below=of esp32] (driver) {Motor\\Driver};

        % Motors branch to the right (compact)
        \node[boxHW, right=16mm of driver, yshift=+9mm] (motorL) {Left\\Motor};
        \node[boxHW, right=16mm of driver, yshift=-9mm] (motorR) {Right\\Motor};

        % Arrows + labels
        \draw[arrow] (phone) -- node[label, right]{Bluetooth SPP} (esp32);
        \draw[arrow] (esp32) -- node[label, right, yshift=-3mm]{PWM + DIR} (driver);

        \draw[arrow] (driver.east) -- node[label, above, sloped]{Power} (motorL.west);
        \draw[arrow] (driver.east) -- node[label, below, sloped]{Power} (motorR.west);

        % Optional subtle grouping
        \draw[dashed, rounded corners=2mm, draw=black!30]
        ($(phone.north west)+(-4mm,4mm)$) rectangle ($(esp32.south east)+(4mm,-4mm)$);
        \node[font=\normalsize, text=black!60] at ($(phone.west)!0.5!(esp32.north)-(2.5,6mm)$) {Control Link};

        \draw[dashed, rounded corners=2mm, draw=black!30]
        ($(driver.north west)+(-4mm,12mm)$) rectangle ($(motorR.south east)+(4mm,-4mm)$);
        \node[font=\normalsize, text=black!60] at ($(driver.north)!0.55!(motorL.north)+(11mm,10mm)$) {Power Stage};

    \end{tikzpicture}
\end{center}

\section{Servo Control Example}

Servos can be connected directly to PWM pins on the ESP32.

\begin{itemize}
	\item Servo signal → ESP32 GPIO pin
	\item Servo power → 5V supply
	\item Servo ground → common ground
\end{itemize}

Example uses:

\begin{itemize}
	\item Steering control
	\item Camera pan control
	\item Robotic arm joints
\end{itemize}

\section{LED and Buzzer Outputs}

Digital outputs can control LEDs or buzzers.

Example connections:

\begin{itemize}
	\item GPIO pin → LED (with resistor)
	\item GPIO pin → buzzer module
\end{itemize}

These outputs can be controlled using buttons or switches in the Android application.

\section{Sensor Connections}

Sensors can be connected using:

\begin{itemize}
	\item Analog inputs
	\item Digital inputs
	\item I2C interface
\end{itemize}

The firmware allows sensor data to be transmitted to the Android application using telemetry.